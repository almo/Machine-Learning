# Gradle
.gradle/
build/
**/*.iml    # IntelliJ IDEA module files
.idea/
!.idea/.gitignore    # Don't ignore the .gitignore within .idea (if you have one)
!.idea/libraries/    # Keep IDE-managed libraries for easier setup
!.idea/workspace.xml # Exclude user-specific workspace settings
!.idea/tasks.xml     # Exclude user-specific task settings
!.idea/gradle.xml    # Exclude Gradle settings which can vary per user
!.idea/assetWizardSettings.xml # Exclude asset wizard settings (Android Studio)
!.idea/dictionaries/ # Exclude user dictionaries
.idea/caches/        # IDE caches
.idea/modules.xml    # Exclude modules XML as it can be recreated/conflicting

# Kotlin specific
.kotlin/             # Kotlin's persistent data directory (do not commit!)
*.class              # Compiled class files
*.jar                # Generated JAR files
*.war                # Generated WAR files (for web apps)
*.zip                # Generated ZIP files
*.log                # Log files

# Local configuration files
local.properties     # Android SDK path, etc. (local to dev machine)

# Sensitive information (add as needed)
*.jks                # Java KeyStore files (for signing apps)
*.keystore           # Generic keystore files
google-services.json # Google/Firebase client configuration (if sensitive, manage carefully)
*.properties.bak     # Backup properties files
/secure/             # If you have a 'secure' folder for API keys etc.

